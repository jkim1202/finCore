spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:mysql://mysql:3306/${MYSQL_DATABASE:fin_core}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${MYSQL_USER:fincore}
    password: ${MYSQL_PASSWORD:fincore}
  data:
    redis:
      host: redis
      port: 6379
